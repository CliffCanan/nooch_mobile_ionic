angular.module('noochApp.statistics-service', ['noochApp.services'])
  .service('statisticsService', function ($http) {
      
      this.getTransferStats = function () {
        //  return $http.get(URLs.getTransaferStats + '?accessToken=' + accessToken + '&memberId=' + memberId);

      };


  })
